
<%if params["history"]["Plugin"] %>
  <% V2::Plugin.where(name => params["history"]["Plugin"]).first.history %>
<%elsif params["history"]["Theme"]%>
    <% V2::Theme.where(name => params["history"]["Theme"]).first.history %>
<%else %>
  <% hash = V2::Test::history_of_test(params["history"]["start_date"], params["history"]["end_date"]) %>
  <%= hash%>
<%end%>

