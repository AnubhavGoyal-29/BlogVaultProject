<%= semantic_form_for :enable_speed, builder: ActiveAdmin::FormBuilder,
    :url => admin_dashboard_enable_speed_path, :method => :post  do |f| %>
  <%= f.inputs "Sync Limits" do %>
    <%= f.input :file, :as => :file %>
    <%= f.input :text, :as => :text %>
  <%end%>
  <%=submit_tag "Set Values"%>
<%end%>
